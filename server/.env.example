# 서버
PORT=3000
NODE_ENV=development

# PostgreSQL (docker compose up 기본값 — 로컬 5432 충돌 회피 위해 5433 매핑)
DATABASE_URL="postgresql://repset:repset@localhost:5433/repset?schema=public"

# JWT (로컬 인증 토대 — ADR-018: 추후 매니지드 인증으로 어댑터 교체)
JWT_SECRET="dev-change-me"
JWT_EXPIRES_IN="15m"
