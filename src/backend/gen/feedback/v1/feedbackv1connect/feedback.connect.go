// @plm SRS-006  개발 피드백 계약 — 앱에서 적은 문제·개선을 PLM 아이디어보드로 보낸다
//
// ─────────────────────────────────────────────────────────────────────────────
// ── 이건 사용자 기능이 아니다 ───────────────────────────────────────────────
// 내부 사람(coworker·admin)만 쓰는 탭이다. 그래서 **서버가 역할로 막는다** —
// 화면이 탭을 숨기는 것은 편의일 뿐 경계가 아니다.
//
// ── 저장소를 새로 만들지 않는다 ─────────────────────────────────────────────
// 피드백은 우리 DB에 쌓지 않고 **PLM 아이디어보드에 그대로 등록**한다. 논의·투표·채택은
// 이미 거기서 돌아간다 — 우리가 상태를 복제하면 두 곳이 어긋나는 순간부터 아무도 안 믿는다.
// 그래서 이 계약에는 목록을 **되읽는** RPC만 있고, 상태를 바꾸는 RPC는 없다(상태는 PLM 소유).
//
// ── 토큰은 서버에만 있다 ────────────────────────────────────────────────────
// PLM 토큰이 앱 번들에 들어가면 회수할 방법이 없다. 앱은 우리 서버에만 말하고,
// 서버가 PLM에 말한다.
// ─────────────────────────────────────────────────────────────────────────────

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: feedback/v1/feedback.proto

package feedbackv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/JWK-company/liftgram/src/backend/gen/feedback/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// FeedbackServiceName is the fully-qualified name of the FeedbackService service.
	FeedbackServiceName = "feedback.v1.FeedbackService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// FeedbackServiceSubmitProcedure is the fully-qualified name of the FeedbackService's Submit RPC.
	FeedbackServiceSubmitProcedure = "/feedback.v1.FeedbackService/Submit"
	// FeedbackServiceListProcedure is the fully-qualified name of the FeedbackService's List RPC.
	FeedbackServiceListProcedure = "/feedback.v1.FeedbackService/List"
)

// FeedbackServiceClient is a client for the feedback.v1.FeedbackService service.
type FeedbackServiceClient interface {
	// 등록. 성공은 **PLM이 id를 줬다**는 뜻이다 — 우리 쪽에 남는 것은 없다.
	Submit(context.Context, *connect.Request[v1.SubmitRequest]) (*connect.Response[v1.SubmitResponse], error)
	// 이 프로젝트의 인앱 피드백 목록. 대시보드에서 손으로 적은 아이디어는 섞이지 않는다.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
}

// NewFeedbackServiceClient constructs a client for the feedback.v1.FeedbackService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFeedbackServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FeedbackServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	feedbackServiceMethods := v1.File_feedback_v1_feedback_proto.Services().ByName("FeedbackService").Methods()
	return &feedbackServiceClient{
		submit: connect.NewClient[v1.SubmitRequest, v1.SubmitResponse](
			httpClient,
			baseURL+FeedbackServiceSubmitProcedure,
			connect.WithSchema(feedbackServiceMethods.ByName("Submit")),
			connect.WithClientOptions(opts...),
		),
		list: connect.NewClient[v1.ListRequest, v1.ListResponse](
			httpClient,
			baseURL+FeedbackServiceListProcedure,
			connect.WithSchema(feedbackServiceMethods.ByName("List")),
			connect.WithClientOptions(opts...),
		),
	}
}

// feedbackServiceClient implements FeedbackServiceClient.
type feedbackServiceClient struct {
	submit *connect.Client[v1.SubmitRequest, v1.SubmitResponse]
	list   *connect.Client[v1.ListRequest, v1.ListResponse]
}

// Submit calls feedback.v1.FeedbackService.Submit.
func (c *feedbackServiceClient) Submit(ctx context.Context, req *connect.Request[v1.SubmitRequest]) (*connect.Response[v1.SubmitResponse], error) {
	return c.submit.CallUnary(ctx, req)
}

// List calls feedback.v1.FeedbackService.List.
func (c *feedbackServiceClient) List(ctx context.Context, req *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return c.list.CallUnary(ctx, req)
}

// FeedbackServiceHandler is an implementation of the feedback.v1.FeedbackService service.
type FeedbackServiceHandler interface {
	// 등록. 성공은 **PLM이 id를 줬다**는 뜻이다 — 우리 쪽에 남는 것은 없다.
	Submit(context.Context, *connect.Request[v1.SubmitRequest]) (*connect.Response[v1.SubmitResponse], error)
	// 이 프로젝트의 인앱 피드백 목록. 대시보드에서 손으로 적은 아이디어는 섞이지 않는다.
	List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error)
}

// NewFeedbackServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFeedbackServiceHandler(svc FeedbackServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	feedbackServiceMethods := v1.File_feedback_v1_feedback_proto.Services().ByName("FeedbackService").Methods()
	feedbackServiceSubmitHandler := connect.NewUnaryHandler(
		FeedbackServiceSubmitProcedure,
		svc.Submit,
		connect.WithSchema(feedbackServiceMethods.ByName("Submit")),
		connect.WithHandlerOptions(opts...),
	)
	feedbackServiceListHandler := connect.NewUnaryHandler(
		FeedbackServiceListProcedure,
		svc.List,
		connect.WithSchema(feedbackServiceMethods.ByName("List")),
		connect.WithHandlerOptions(opts...),
	)
	return "/feedback.v1.FeedbackService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FeedbackServiceSubmitProcedure:
			feedbackServiceSubmitHandler.ServeHTTP(w, r)
		case FeedbackServiceListProcedure:
			feedbackServiceListHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFeedbackServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedFeedbackServiceHandler struct{}

func (UnimplementedFeedbackServiceHandler) Submit(context.Context, *connect.Request[v1.SubmitRequest]) (*connect.Response[v1.SubmitResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("feedback.v1.FeedbackService.Submit is not implemented"))
}

func (UnimplementedFeedbackServiceHandler) List(context.Context, *connect.Request[v1.ListRequest]) (*connect.Response[v1.ListResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("feedback.v1.FeedbackService.List is not implemented"))
}
