// @plm SRS-039 @plm SRS-040  착용장비 계약 — 설정 조회와 클릭 집계 (ADR-027)
//
// ─────────────────────────────────────────────────────────────────────────────
// ── 왜 앱이 링크를 만들지 않나 ──────────────────────────────────────────────
// 제휴 딥링크와 활성 스위치는 **서버 설정에만** 둔다. 앱 번들에 넣으면 평문으로 굳어 회수가 불가능하고,
// 링크를 바꾸려면 앱을 다시 배포해야 한다(ADR-027 D2). 그래서 서버는 **완성된 링크만** 내려보내고,
// 파트너 태그·서브아이디 같은 조립 재료는 절대 싣지 않는다.
//
// ── Phase 0은 수수료를 벌지 않는다 ──────────────────────────────────────────
// 지금 단계가 남기는 자산은 **클릭 데이터뿐**이다. 그 수치가 다음 단계(자동 감지·정산) 투자 판단의
// 근거가 된다 — 그래서 집계 RPC가 계약에 있다.
//
// ── 기본값은 꺼짐이다 ───────────────────────────────────────────────────────
// 설정이 없으면 `enabled=false`, 링크도 없다. 미등록 매체에 광고를 노출하면 제재 대상이라,
// **켜려는 의도가 명확할 때만** 켜져야 한다.
// ─────────────────────────────────────────────────────────────────────────────

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gear/v1/gear.proto

package gearv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/JWK-company/liftgram/src/backend/gen/gear/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GearServiceName is the fully-qualified name of the GearService service.
	GearServiceName = "gear.v1.GearService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GearServiceGetConfigProcedure is the fully-qualified name of the GearService's GetConfig RPC.
	GearServiceGetConfigProcedure = "/gear.v1.GearService/GetConfig"
	// GearServiceRecordClickProcedure is the fully-qualified name of the GearService's RecordClick RPC.
	GearServiceRecordClickProcedure = "/gear.v1.GearService/RecordClick"
)

// GearServiceClient is a client for the gear.v1.GearService service.
type GearServiceClient interface {
	// 화면이 링크를 열기 전에 한 번 읽는다. 실패해도 화면은 검색 링크로 동작한다.
	GetConfig(context.Context, *connect.Request[v1.GetConfigRequest]) (*connect.Response[v1.GetConfigResponse], error)
	// 링크를 **연 뒤에** 부른다(막지 않는다). 같은 사람이 같은 글의 같은 장비를
	// 짧은 시간에 여러 번 눌러도 한 번만 쌓인다 — 반복 클릭은 제재 사유이기도 하다.
	RecordClick(context.Context, *connect.Request[v1.RecordClickRequest]) (*connect.Response[v1.RecordClickResponse], error)
}

// NewGearServiceClient constructs a client for the gear.v1.GearService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGearServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GearServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gearServiceMethods := v1.File_gear_v1_gear_proto.Services().ByName("GearService").Methods()
	return &gearServiceClient{
		getConfig: connect.NewClient[v1.GetConfigRequest, v1.GetConfigResponse](
			httpClient,
			baseURL+GearServiceGetConfigProcedure,
			connect.WithSchema(gearServiceMethods.ByName("GetConfig")),
			connect.WithClientOptions(opts...),
		),
		recordClick: connect.NewClient[v1.RecordClickRequest, v1.RecordClickResponse](
			httpClient,
			baseURL+GearServiceRecordClickProcedure,
			connect.WithSchema(gearServiceMethods.ByName("RecordClick")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gearServiceClient implements GearServiceClient.
type gearServiceClient struct {
	getConfig   *connect.Client[v1.GetConfigRequest, v1.GetConfigResponse]
	recordClick *connect.Client[v1.RecordClickRequest, v1.RecordClickResponse]
}

// GetConfig calls gear.v1.GearService.GetConfig.
func (c *gearServiceClient) GetConfig(ctx context.Context, req *connect.Request[v1.GetConfigRequest]) (*connect.Response[v1.GetConfigResponse], error) {
	return c.getConfig.CallUnary(ctx, req)
}

// RecordClick calls gear.v1.GearService.RecordClick.
func (c *gearServiceClient) RecordClick(ctx context.Context, req *connect.Request[v1.RecordClickRequest]) (*connect.Response[v1.RecordClickResponse], error) {
	return c.recordClick.CallUnary(ctx, req)
}

// GearServiceHandler is an implementation of the gear.v1.GearService service.
type GearServiceHandler interface {
	// 화면이 링크를 열기 전에 한 번 읽는다. 실패해도 화면은 검색 링크로 동작한다.
	GetConfig(context.Context, *connect.Request[v1.GetConfigRequest]) (*connect.Response[v1.GetConfigResponse], error)
	// 링크를 **연 뒤에** 부른다(막지 않는다). 같은 사람이 같은 글의 같은 장비를
	// 짧은 시간에 여러 번 눌러도 한 번만 쌓인다 — 반복 클릭은 제재 사유이기도 하다.
	RecordClick(context.Context, *connect.Request[v1.RecordClickRequest]) (*connect.Response[v1.RecordClickResponse], error)
}

// NewGearServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGearServiceHandler(svc GearServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gearServiceMethods := v1.File_gear_v1_gear_proto.Services().ByName("GearService").Methods()
	gearServiceGetConfigHandler := connect.NewUnaryHandler(
		GearServiceGetConfigProcedure,
		svc.GetConfig,
		connect.WithSchema(gearServiceMethods.ByName("GetConfig")),
		connect.WithHandlerOptions(opts...),
	)
	gearServiceRecordClickHandler := connect.NewUnaryHandler(
		GearServiceRecordClickProcedure,
		svc.RecordClick,
		connect.WithSchema(gearServiceMethods.ByName("RecordClick")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gear.v1.GearService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GearServiceGetConfigProcedure:
			gearServiceGetConfigHandler.ServeHTTP(w, r)
		case GearServiceRecordClickProcedure:
			gearServiceRecordClickHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGearServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGearServiceHandler struct{}

func (UnimplementedGearServiceHandler) GetConfig(context.Context, *connect.Request[v1.GetConfigRequest]) (*connect.Response[v1.GetConfigResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gear.v1.GearService.GetConfig is not implemented"))
}

func (UnimplementedGearServiceHandler) RecordClick(context.Context, *connect.Request[v1.RecordClickRequest]) (*connect.Response[v1.RecordClickResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gear.v1.GearService.RecordClick is not implemented"))
}
