// 운영 메타 계약 — "어디서 도는가"를 답하는 도메인 밖의 서비스
//
// 도메인 계약(운동 카탈로그 등)에 이걸 얹지 않는 이유: 인스턴스 식별자와 propagation 버스는
// **도메인 데이터가 아니라 실행 환경의 사실**이다. 도메인에 섞으면 "왜 카탈로그 계약이 redis를
// 아는가"가 되고, 그 도메인을 걷어낼 때 운영 화면이 함께 깨진다.
//
// 다중 인스턴스 propagation을 눈으로 확인할 때(make scale-2) 이 값이 근거가 된다.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: meta/v1/meta.proto

package metav1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/JWK-company/liftgram/src/backend/gen/meta/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MetaServiceName is the fully-qualified name of the MetaService service.
	MetaServiceName = "meta.v1.MetaService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MetaServiceGetMetaProcedure is the fully-qualified name of the MetaService's GetMeta RPC.
	MetaServiceGetMetaProcedure = "/meta.v1.MetaService/GetMeta"
)

// MetaServiceClient is a client for the meta.v1.MetaService service.
type MetaServiceClient interface {
	GetMeta(context.Context, *connect.Request[v1.GetMetaRequest]) (*connect.Response[v1.GetMetaResponse], error)
}

// NewMetaServiceClient constructs a client for the meta.v1.MetaService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMetaServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MetaServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	metaServiceMethods := v1.File_meta_v1_meta_proto.Services().ByName("MetaService").Methods()
	return &metaServiceClient{
		getMeta: connect.NewClient[v1.GetMetaRequest, v1.GetMetaResponse](
			httpClient,
			baseURL+MetaServiceGetMetaProcedure,
			connect.WithSchema(metaServiceMethods.ByName("GetMeta")),
			connect.WithClientOptions(opts...),
		),
	}
}

// metaServiceClient implements MetaServiceClient.
type metaServiceClient struct {
	getMeta *connect.Client[v1.GetMetaRequest, v1.GetMetaResponse]
}

// GetMeta calls meta.v1.MetaService.GetMeta.
func (c *metaServiceClient) GetMeta(ctx context.Context, req *connect.Request[v1.GetMetaRequest]) (*connect.Response[v1.GetMetaResponse], error) {
	return c.getMeta.CallUnary(ctx, req)
}

// MetaServiceHandler is an implementation of the meta.v1.MetaService service.
type MetaServiceHandler interface {
	GetMeta(context.Context, *connect.Request[v1.GetMetaRequest]) (*connect.Response[v1.GetMetaResponse], error)
}

// NewMetaServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMetaServiceHandler(svc MetaServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	metaServiceMethods := v1.File_meta_v1_meta_proto.Services().ByName("MetaService").Methods()
	metaServiceGetMetaHandler := connect.NewUnaryHandler(
		MetaServiceGetMetaProcedure,
		svc.GetMeta,
		connect.WithSchema(metaServiceMethods.ByName("GetMeta")),
		connect.WithHandlerOptions(opts...),
	)
	return "/meta.v1.MetaService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MetaServiceGetMetaProcedure:
			metaServiceGetMetaHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMetaServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMetaServiceHandler struct{}

func (UnimplementedMetaServiceHandler) GetMeta(context.Context, *connect.Request[v1.GetMetaRequest]) (*connect.Response[v1.GetMetaResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("meta.v1.MetaService.GetMeta is not implemented"))
}
