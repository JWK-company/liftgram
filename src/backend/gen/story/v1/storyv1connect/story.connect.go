// @plm SRS-019  스토리 계약 — 24시간 뒤 사라지는 한 컷
//
// ─────────────────────────────────────────────────────────────────────────────
// 글(피드)과 다른 점은 **수명**뿐이 아니다:
//   · 24시간이 지나면 안 보인다. **지우지는 않는다** — 신고·감사 때문에 기록은 남는다.
//   · 사람별로 묶어서 본다(한 사람의 여러 컷이 한 타일). 그래서 목록이 아니라 **그룹**을 돌려준다.
//   · **내 그룹이 항상 맨 앞이다** — 트레이 첫 칸이 "내 스토리"이기 때문이다.
//
// ── 누가 본 것인가 ──────────────────────────────────────────────────────────
// 열람 여부는 **서버가 모른다.** 기기에 남긴다(app도 그랬다 — `story_seen_v1`).
// 안 본 표시(강조 링)는 개인적인 표시라 기기마다 달라도 되고, 서버에 두면 쓰기가 폭증한다
// (한 사람이 스토리를 넘길 때마다 쓰기가 생긴다).
//
// ── 사진은 먼저 올린다 ──────────────────────────────────────────────────────
// 여기서는 바이트를 받지 않는다. `media.v1.UploadImage`로 먼저 올리고 그 주소를 싣는다 —
// 업로드는 성공했는데 스토리 만들기가 실패했을 때 사진을 다시 올리지 않아도 되게.
// 주소는 **내가 올린 것**이어야 한다(남의 사진 주소를 실을 수 없다 — 규칙은 service가 지킨다).
// ─────────────────────────────────────────────────────────────────────────────

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: story/v1/story.proto

package storyv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/JWK-company/liftgram/src/backend/gen/story/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StoryServiceName is the fully-qualified name of the StoryService service.
	StoryServiceName = "story.v1.StoryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StoryServiceCreateStoryProcedure is the fully-qualified name of the StoryService's CreateStory
	// RPC.
	StoryServiceCreateStoryProcedure = "/story.v1.StoryService/CreateStory"
	// StoryServiceListActiveStoriesProcedure is the fully-qualified name of the StoryService's
	// ListActiveStories RPC.
	StoryServiceListActiveStoriesProcedure = "/story.v1.StoryService/ListActiveStories"
)

// StoryServiceClient is a client for the story.v1.StoryService service.
type StoryServiceClient interface {
	CreateStory(context.Context, *connect.Request[v1.CreateStoryRequest]) (*connect.Response[v1.CreateStoryResponse], error)
	// 내 것 + 내가 팔로우하는 사람의 것 중 아직 만료되지 않은 것.
	ListActiveStories(context.Context, *connect.Request[v1.ListActiveStoriesRequest]) (*connect.Response[v1.ListActiveStoriesResponse], error)
}

// NewStoryServiceClient constructs a client for the story.v1.StoryService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStoryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StoryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	storyServiceMethods := v1.File_story_v1_story_proto.Services().ByName("StoryService").Methods()
	return &storyServiceClient{
		createStory: connect.NewClient[v1.CreateStoryRequest, v1.CreateStoryResponse](
			httpClient,
			baseURL+StoryServiceCreateStoryProcedure,
			connect.WithSchema(storyServiceMethods.ByName("CreateStory")),
			connect.WithClientOptions(opts...),
		),
		listActiveStories: connect.NewClient[v1.ListActiveStoriesRequest, v1.ListActiveStoriesResponse](
			httpClient,
			baseURL+StoryServiceListActiveStoriesProcedure,
			connect.WithSchema(storyServiceMethods.ByName("ListActiveStories")),
			connect.WithClientOptions(opts...),
		),
	}
}

// storyServiceClient implements StoryServiceClient.
type storyServiceClient struct {
	createStory       *connect.Client[v1.CreateStoryRequest, v1.CreateStoryResponse]
	listActiveStories *connect.Client[v1.ListActiveStoriesRequest, v1.ListActiveStoriesResponse]
}

// CreateStory calls story.v1.StoryService.CreateStory.
func (c *storyServiceClient) CreateStory(ctx context.Context, req *connect.Request[v1.CreateStoryRequest]) (*connect.Response[v1.CreateStoryResponse], error) {
	return c.createStory.CallUnary(ctx, req)
}

// ListActiveStories calls story.v1.StoryService.ListActiveStories.
func (c *storyServiceClient) ListActiveStories(ctx context.Context, req *connect.Request[v1.ListActiveStoriesRequest]) (*connect.Response[v1.ListActiveStoriesResponse], error) {
	return c.listActiveStories.CallUnary(ctx, req)
}

// StoryServiceHandler is an implementation of the story.v1.StoryService service.
type StoryServiceHandler interface {
	CreateStory(context.Context, *connect.Request[v1.CreateStoryRequest]) (*connect.Response[v1.CreateStoryResponse], error)
	// 내 것 + 내가 팔로우하는 사람의 것 중 아직 만료되지 않은 것.
	ListActiveStories(context.Context, *connect.Request[v1.ListActiveStoriesRequest]) (*connect.Response[v1.ListActiveStoriesResponse], error)
}

// NewStoryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStoryServiceHandler(svc StoryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	storyServiceMethods := v1.File_story_v1_story_proto.Services().ByName("StoryService").Methods()
	storyServiceCreateStoryHandler := connect.NewUnaryHandler(
		StoryServiceCreateStoryProcedure,
		svc.CreateStory,
		connect.WithSchema(storyServiceMethods.ByName("CreateStory")),
		connect.WithHandlerOptions(opts...),
	)
	storyServiceListActiveStoriesHandler := connect.NewUnaryHandler(
		StoryServiceListActiveStoriesProcedure,
		svc.ListActiveStories,
		connect.WithSchema(storyServiceMethods.ByName("ListActiveStories")),
		connect.WithHandlerOptions(opts...),
	)
	return "/story.v1.StoryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StoryServiceCreateStoryProcedure:
			storyServiceCreateStoryHandler.ServeHTTP(w, r)
		case StoryServiceListActiveStoriesProcedure:
			storyServiceListActiveStoriesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStoryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedStoryServiceHandler struct{}

func (UnimplementedStoryServiceHandler) CreateStory(context.Context, *connect.Request[v1.CreateStoryRequest]) (*connect.Response[v1.CreateStoryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("story.v1.StoryService.CreateStory is not implemented"))
}

func (UnimplementedStoryServiceHandler) ListActiveStories(context.Context, *connect.Request[v1.ListActiveStoriesRequest]) (*connect.Response[v1.ListActiveStoriesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("story.v1.StoryService.ListActiveStories is not implemented"))
}
