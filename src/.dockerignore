# 이미지에 들어가면 안 되는 것들.
# 특히 .env — 개발용 접속 정보가 이미지에 박혀 나가면
#   ① 자격증명이 이미지와 함께 유출되고
#   ② 프로덕션에서 환경변수를 빠뜨려도 조용히 뜬다(개발 DB를 보게 된다).
# 설정은 오직 실행 시 환경변수로만 들어온다(컨테이너 실행 규약).
.env
.env.*
!.env.example

node_modules
.next
.git
*.log

# 빌드에 불필요한 것
docs
.github
*.md
!README.md
